# Add this line to your /etc/services for the new CALENDAR application:
# (Must match CMDPORT position in bpq32.cfg)

calendar        63140/tcp                       # Club event calendar viewer (HOST 14)

# After adding, reload inetd:
# sudo killall -HUP inetd
