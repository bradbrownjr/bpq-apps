# Add these lines to your /etc/services for the new applications:
# (Must match CMDPORT positions in bpq32.cfg)

calendar        63140/tcp                       # Club event calendar viewer (HOST 14)
hamqsl          63150/tcp                       # Callsign QSL card lookup (HOST 15)
hamtest         63160/tcp                       # Amateur radio exam practice (HOST 16)

# After adding, reload inetd:
# sudo killall -HUP inetd
