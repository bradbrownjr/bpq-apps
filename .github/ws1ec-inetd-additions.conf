# Add this line to your /etc/inetd.conf for the new CALENDAR application:
# (Adjust user and paths as needed for your system)

calendar                stream  tcp     nowait  ect     /home/ect/apps/calendar.py

# After adding, reload inetd:
# sudo killall -HUP inetd
